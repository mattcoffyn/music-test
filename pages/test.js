const AudioContext = window.AudioContext || window.webkitAudioContext;

const audioContext = new AudioContext();

const Test = () => {
  return (
    <div>
      <h1>Test</h1>
    </div>
  );
};

export default Test;
